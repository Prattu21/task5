# task5
### INNER JOIN
Returns rows that have matching values in both tables.

### LEFT JOIN
Returns all rows from the left table (Customers) and matching rows from the right (Orders).

### RIGHT JOIN 
Returns all rows from the right table (Orders), and matching rows from the left (Customers).

### FULL JOIN
Shows all rows from both tables (matches + non-matches).
